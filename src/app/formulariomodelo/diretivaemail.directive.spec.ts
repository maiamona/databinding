import { DiretivaemailDirective } from './diretivaemail.directive';

describe('DiretivaemailDirective', () => {
  it('should create an instance', () => {
    const directive = new DiretivaemailDirective();
    expect(directive).toBeTruthy();
  });
});
